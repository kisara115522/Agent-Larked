{
  "name": "agent-larked",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/sdk",
    "packages/cli",
    "packages/mcp",
    "packages/web"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "tsc --noEmit",
    "test": "npm test --workspaces --if-present",
    "reset:kisara": "tsx scripts/reset-kisara-admin.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7"
  }
}
